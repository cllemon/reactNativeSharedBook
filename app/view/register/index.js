import React, { Component } from 'react';
import { Text, View, TextInput, Button, Slider, Switch } from 'react-native';

class Register extends Component {
  /**
   *
   *
   * @returns
   * @memberof Register
   */
  render() {
    return <View />;
  }
}

export default Register;
